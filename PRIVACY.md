# Privacy

Kairo is local-only and makes no network requests. Captures are written to the current vault or retained in local Obsidian plugin data until delivery. Queue data is not encrypted. Diagnostics omit captured text. See the repository `PRIVACY.md` for the full threat model.
